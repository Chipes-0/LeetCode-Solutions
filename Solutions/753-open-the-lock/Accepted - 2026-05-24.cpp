#include <queue>
#include <vector>
#include <string>

class Solution {
public:
    int openLock(vector<string>& deadends, string target) {
        queue<vector<int>> q;
        vector<bool> visited(10000, false);
        q.push();
        v.push_back(0)
        
        return -1;
    }
};